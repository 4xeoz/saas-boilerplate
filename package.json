{
  "name": "saas-boilerplate",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\" --names \"backend,frontend\" --prefix-colors \"cyan,magenta\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "type-check": "npm run type-check -w backend && npm run type-check -w frontend"
  },
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "packageManager": "npm@10.9.2"
}
